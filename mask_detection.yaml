# YOLO config files for Object Detection

# The following are relative to your paths directory, make sure it's pointing at the right directory

# path: 'ultralytics\settings.py' 
train: 'data_od\images\train' # relative to path
val: 'data_od\images\val'   # relative to path

nc: 2 # number of classes
names: ['mask', 'no-mask']
